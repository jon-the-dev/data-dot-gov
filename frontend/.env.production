# Production API Configuration
# This file is used during production builds

# Production API URL - uses relative path to work through Traefik proxy
# The /api/v1 path is proxied by Traefik to the backend service
VITE_API_URL=/api/v1
